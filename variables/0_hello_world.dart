// main 함수에 따라서 실행을 할 수 있다.
// main method가 반드시 존재해야 한다.
void main() {
    print('hello world');
    // 세미콜론도 반드시 필요하다
    // 없으면 에러가 발생한다. 보통 js 같은 경우 autoformatter에서 자동으로 붙여주지만 dart는 그렇지 않다.
    // 컴파일이 되지 않는다.
}